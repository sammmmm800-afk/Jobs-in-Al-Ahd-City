# مواصفات بناء صفحة "النماذج الإدارية" — كراج الميكانيك

هذا الملف مكتوب ليُعطى مباشرة لأداة ذكاء اصطناعي برمجية (مثل Claude Code في VS Code) لتنفيذ الصفحة بدقة.

## الفكرة العامة
صفحة محمية بكلمة مرور، تحتوي على 5 تبويبات رئيسية، كل تبويب فيه نموذج (form) يولّد **نص تقرير جاهز** بالضغط على زر "إنشاء التقرير"، ثم زر "نسخ التقرير" ينسخ هذا النص كاملاً إلى الحافظة (clipboard) بضغطة واحدة، بنفس آلية مولّد "المسمى الميداني" الموجود في الصفحة الرئيسية (نص يظهر في صندوق، وزر ينسخه بـ navigator.clipboard.writeText).

## بوابة كلمة المرور
- عنوان: "محتوى محمي"
- نص فرعي: "يرجى إدخال كلمة المرور للوصول إلى هذا القسم"
- حقل إدخال كلمة مرور (type=password)
- رسالة خطأ تظهر عند كلمة مرور خاطئة: "كلمة المرور غير صحيحة، يرجى المحاولة مرة أخرى"
- زرّان: "تأكيد" و"رجوع" (رجوع يوجّه إلى index.html)
- بعد إدخال كلمة المرور الصحيحة تُخفى البوابة ويظهر محتوى التقارير (تخزين الحالة في الذاكرة فقط، بدون localStorage، لأنها حماية واجهة فقط وليست حماية حقيقية من جهة الخادم)

## التبويبات الرئيسية (بالترتيب)
1. تقارير نائب الموجه
2. تقارير إشراف منطقة
3. تقرير التعديل والتزويد
4. القرارات الإدارية (يحتوي 6 تبويبات فرعية)
5. تقارير التوظيف

## آلية تنسيق الحقول الخاصة
- **حقول "User ID"**: عند الكتابة تُنسّق تلقائيًا لتقبل أرقامًا فقط (strip أي حرف غير رقمي أثناء الكتابة). عند توليد التقرير تُكتب بالشكل: `#<الرقم>` (مثال: أدخل 1234 → يظهر في التقرير #1234). إذا كان الحقل فارغًا يظهر "-".
- **حقول "الكود" (مثل لوس/ساندي/بوليتو)**: البلايسهولدر "أدخل الكود فقط (مثال: G-000)". عند الخروج من الحقل (blur): إذا كان الإدخال أرقامًا فقط، حوّله إلى `G-XXX` (padStart بثلاث خانات، مثال: 5 → G-005). إذا كتب المستخدم "G-" مسبقًا يُترك كما هو.
- **حقول الساعة**: input نصي أو time + مجموعة أزرار توجيه (segmented control) لاختيار "ص" أو "م" بجانبه (radio button مموّه كزرّين).
- **حقل اليوم**: select بالخيارات: اختر اليوم (افتراضي) / الأحد / الاثنين / الثلاثاء / الأربعاء / الخميس / الجمعة / السبت.
- **أسئلة نعم/لا**: زرّان (نعم/لا) بشكل toggle، وليس select.
- **حقل المستوى السابق**: select بالخيارات: فني متدرب، مستوى 1 إلى مستوى 10.

## نموذج 1: تقرير نائب الموجه اليومي
الحقول بالترتيب:
- رقم التقرير (رقم)
- اليوم (select الأيام)
- الساعة (وقت + ص/م)
- التاريخ (date)
--- قسم: قيادة الكراج (كل حقل من نوع User ID) ---
- مستشار عام
- المشرف العام
- مشرفين المناطق
- الموجه الميداني
- نائب الموجه
- مركز الاتصالات
--- قسم: لاعب معتمد (كل حقل من نوع كود G-000) ---
- لوس
- ساندي
- بوليتو
- فني ميناء
- اسطول لوس
- اسطول بوليتو
- تسجيل خروج
- زر "إنشاء التقرير" ثم زر "نسخ التقرير"

نص التقرير الناتج المقترح (عدّله إذا وصلك التنسيق الرسمي الفعلي):
```
📋 تقرير نائب الموجه اليومي
رقم التقرير: {رقم التقرير}
اليوم: {اليوم} — {التاريخ}
الساعة: {الساعة} {ص/م}

▬ قيادة الكراج
مستشار عام: {#..}
المشرف العام: {#..}
مشرفين المناطق: {#..}
الموجه الميداني: {#..}
نائب الموجه: {#..}
مركز الاتصالات: {#..}

▬ لاعب معتمد
لوس: {G-..}
ساندي: {G-..}
بوليتو: {G-..}
فني ميناء: {G-..}
اسطول لوس: {G-..}
اسطول بوليتو: {G-..}
تسجيل خروج: {G-..}
```

## نموذج 2: تقارير إشراف منطقة
- رقم التقرير
- اسم المنطقة
- من الساعة (وقت + ص/م) / إلى الساعة (وقت + ص/م)
--- قسم: فريق الإشراف ---
- المشرف (User ID) + الاسم (نص حر)
- الموجه الميداني (User ID)
- نائب الموجه (User ID)
- مركز الاتصالات (User ID)
--- قسم: الملاحظات (كلها textarea) ---
- ملاحظات إيجابية
- ملاحظات الموجه الميداني
- ملاحظات نائب الموجه
- ملاحظات مركز الاتصالات
- ملاحظات سلبية
- زر إنشاء + نسخ

## نموذج 3: تقرير التعديل والتزويد
- اليوم (select) / التاريخ / الساعة (+ص/م)
--- قسم عام ---
- التقرير من (User ID)
- مسؤول التعديل والتزويد (User ID)
- إدارة الكراج (User ID)
--- قسم: المسارات (10 حقول User ID) ---
مسار 1 إلى مسار 10
--- قسم: مسارات الشاحنات (2 حقول User ID) ---
مسار شاحنات 1، مسار شاحنات 2
--- قسم: تنظيم السير (4 حقول User ID) ---
تنظيم سير 1 إلى 4
--- حقل نصي حر ---
- وحدات الدعم في القطاعات الأخرى (textarea)
- زر إنشاء + نسخ
(عند التوليد: تجاهل أي مسار فارغ ولا تكتب سطره في النص النهائي)

## نموذج 4: تقارير التوظيف
- رقم الدفعة
--- قسم: الفريق الإداري (User ID) ---
إدارة الكراج، المسؤول العام للشؤون الخارجية والداخلية، المدقق، إدارة تسجيل المتقدمين، المحاضر
--- قسم: المقابلات (8 حقول User ID) ---
مقابلة 1 إلى مقابلة 8
--- قسم عام ---
مسؤول المنظمين (User ID)
--- قسم: تنظيم الحشود (3 حقول User ID) ---
تنظيم الحشود 1 إلى 3
--- قسم: سائقي الباص (3 حقول User ID) ---
سائق باص 1 إلى 3
--- حقل ---
وحدات معفية (User ID)
- زر إنشاء + نسخ
(تجاهل الحقول الفارغة عند توليد النص، تمامًا كما في نموذج المسارات)

## القرارات الإدارية (6 تبويبات فرعية)

**1. إجازة داخلية**: الفني (User ID) / المدة بالساعات / من الساعة (+ص/م) / إلى الساعة (+ص/م) / الرصيد المتبقي بالساعات → زر "إنشاء النموذج"

**2. إجازة خارجية**: الفني / المدة بالأيام / من تاريخ / إلى تاريخ → إنشاء النموذج

**3. تمديد إجازة**: الفني / مدة التمديد بالأيام / إلى تاريخ → إنشاء النموذج

**4. قطع إجازة**: الفني فقط → إنشاء النموذج

**5. استقالة**: الفني / السبب / المستوى السابق (select) / الاسم الكامل / الكود الميداني / هل يوجد مخالفات سابقة؟ (نعم/لا) / هل تم سحبها؟ (نعم/لا) → إنشاء النموذج

**6. فصل من الخدمة**: نفس حقول الاستقالة (بدون السبب) + العقوبة الإجمالية + سبب الفصل → إنشاء النموذج + نسخ النموذج

كل نموذج من هذه الستة له صندوق نتيجة ونسخ منفصل، بنفس آلية "نسخ التقرير".

## آلية النسخ (موحّدة لكل النماذج)
```js
async function copyReport(text, btn) {
  try {
    await navigator.clipboard.writeText(text);
    const original = btn.textContent;
    btn.textContent = "تم النسخ ✔";
    setTimeout(() => { btn.textContent = original; }, 1500);
  } catch (e) {
    alert("تعذر النسخ، انسخ النص يدويًا:\n" + text);
  }
}
```
كل زر "إنشاء" يبني نص التقرير من قيم النموذج (بنفس الترتيب أعلاه)، يعرضه داخل `<pre>` بخط عادي RTL، ثم يُفعّل زر النسخ المرتبط به.

## ملاحظة مهمة
النصوص المقترحة أعلاه لشكل التقرير النهائي هي **اجتهاد مبني على أسماء الحقول فقط**، وليست منسوخة من الموقع الأصلي (تعذّر الوصول لكود الجافاسكربت الفعلي). إذا كان عندك نسخة من نص تقرير حقيقي تم توليده سابقًا (سكرين شوت أو نص منسوخ)، أرسله وسأطابق الصياغة والرموز بالضبط.
